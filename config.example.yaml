# Example configuration for the CG RERA extraction orchestrator.
run:
  mode: FULL
  search_filters:
    districts:
      - Raipur
      - Durg
    statuses:
      - Registered
      - Completed
    project_types:
      - Residential
      - Commercial
  output_base_dir: ./outputs/demo-run
  state_code: CG
browser:
  driver: playwright
  headless: true
  slow_mo_ms: 250
