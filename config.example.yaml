# Example configuration for the CG RERA extraction orchestrator.
run:
  crawl_mode: full
  max_projects: 25
  output_dir: outputs/demo-run
  search_filters:
    - district: Raipur
      status: Registered
    - district: Durg
      status: Completed
browser:
  provider: playwright
  headless: true
  slow_mo_ms: 250
  manual_captcha: true
data_dir: data
